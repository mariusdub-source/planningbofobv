const express = require('express');
const app = express();

app.get('/', (req, res) => {
  res.json({
    name: 'MetaFlight AI API',
    status: 'online'
  });
});

app.listen(3001, () => {
  console.log('API running on port 3001');
});
